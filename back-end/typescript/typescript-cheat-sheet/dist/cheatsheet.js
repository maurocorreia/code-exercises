"use strict";
/*          TypeScript CHEAT SHEET          */
// Common packages and commands.
/*
npx tsc --init - To generate tsconfig.json file.
npx tsc fileName.ts - To transpile a .ts file into a .js file.

@tsconfig/node14
@types/node

*/
// Primitive types.
const truth = true;
const zero = 0;
const phrase = "Phrase.";
/* Void is used when there's no return in a function. */
function hello() {
    console.log('Hi');
}
